<p id="notice"><%= notice %></p>
<div class="well">
	<p>
  		<center><h1 class="bg-danger"><strong><%= @post.name %></strong></h1></center>
	</p>

	<p>
  		<%= @post.content.html_safe %>
	</p>
</div>
<%= link_to("Anterior", @post.previous_post,class: 'btn btn-default') if @post.previous_post %>	
<%= link_to("Siguiente", @post.next_post,class: 'pull-right btn btn-default') if @post.next_post %>